# CC session — 2026-05-22 (cashflow-truth era resume)

> Running session memory. Updated DURING the work, not after. Future sessions read this to resume.

**Tip-of-main entering session:** `a32b0a8` — Sub-bundle 1 Commit 4b (TO RECOVER block + 7-day burn card + bills-correction reveal). Sub-bundle 1 SHIPPED in 5 commits (e322ff3 → a32b0a8).

**Live oracle in use this session:** `tests/state-dump/live-2026-05-21.json` — bal $802.66, 227 txns, 16 paidBills entries, paydayReceivedDate 2026-05-14, autodebitProcessingStartTs 2026-05-19, bonus $1,341 still `included:false` despite confirmed status, sole unpaid debt = Property Deposit (via Mum) viaRent. **2 days stale** vs John's phone reality.

---

## Step 1 — Fresh state pull status

**BLOCKED.** `npm run fixture:fresh` requires Phase A device token; CC doesn't autonomously hold John's auth (standing rule). Command surfaced to John:

```
SLYGHT_DEVICE_TOKEN=<43-char from app localStorage["slyght_device_token"]> npm run fixture:fresh
```

Proceeding with the May 21 oracle for current-reality analysis. Drone scoping (Step 2) does not depend on fresh state — it reads HEAD code.

---

## Step 1 — Live dashboard screenshot status

**Deferred.** Three flagged items (net-worth placement, duplicate +Add Debt button, MAX PER DAY mixed signal) are John's real-phone observations — not requiring CC-side capture for code-scoping. Drone B/A/C investigate the render path directly from `index.html`. Screenshot can be re-attempted after fresh pull if useful for verification.

---

## Step 2 — Parallel scoping pipeline

**Tier-1 drones launching in parallel:**

- **Drone A** — net-worth render path. Where computed, where rendered under the coral hero, blast radius of moving/demoting/shrinking.
- **Drone B** — duplicate "+Add Debt" button. Root cause of double render, minimal fix shape, sibling double-renders elsewhere.
- **Drone C** — MAX PER DAY card. Which of Theme B's D3 daily-spend duplicates feeds it, relation to hero number, collapse-or-rework option.
- **Drone D** — Sub-bundle 2 scoping. Log-time interceptor — where it hooks (QuickLog / + button), how it reads burn/headroom to gate, what the warning surface is.
- **Drone E** — P0 verification. Confirm P0-1 (payBillNow double-debit L19644), P0-2 (knownUpcoming double-subtract L20906/20920), P0-3 (cycleEndDate UTC-shift). Are they still live or did Sub-bundle 1 already address them?

Tier-2 auditor follows. Tier-3 collector cross-references against the existing theme map (Themes A-M).

---

## Decisions log (during, not after)

**2026-05-23 — Staleness rule (John):** Investigations are INVALID if they reproduce numbers against `live-2026-05-21.json`. That fixture is stale; phone state has diverged. Only state-independent / structural findings count. Drones K, L, M redirected mid-flight; Drone C's $20 ≠ fixture $0 numeric reproduction is dropped (structural finding — MAX PER DAY uses an independent reader — survives).

**2026-05-23 — Wrangler pull blocker:** Direct KV pull via `npx wrangler kv key get device:366fcb8c...:state-full-snapshot --remote` returned `401 Unauthorized`. No CF account creds in this CC environment. Options surfaced for John (see below).

**2026-05-23 — License granted (John):** "If there is a more effective way you can think of to do something better, bring it up to me in the SDD. literally anything." Tracking ideas in §Could-Do-Better below.

---

## Could-Do-Better — running list (per John's license 2026-05-23)

Anything CC notices during the investigation that could be a better way to do things. Carried into the final SDD as proposals for John's triage.

- **CDB-01** — `BRAIN.chat.dispatchAction(action)` substrate (Drone F finding): single chokepoint replacing executeChatAction switch; schema-validates each action; dispatches through registry where each action declares canonical writer + source tag + audit envelope. Closes Theme H (XR-1 + XR-2 = 4 findings → 1 fix). Higher-leverage than Sub-bundle 2 in terms of money-truth integrity per touch.

- **CDB-02** — `toLocalISODate(d)` + `_isoToday()` helpers (Drone G finding): co-locate with cycle helpers ~L14180; 20 LOCAL-MIDNIGHT-BUG sites enumerated. Pair with new Guardian rule `no-iso-truncate-on-local-date` (allow-block discipline for genuine UTC-OK / STORAGE-KEY sites). One ADR-G covers all sites.

- **CDB-03** — Vocabulary registry as living doc + Guardian rule (Drone I): the disambiguation table (Buffer/Locked/$X/day/Covered/Cleared) becomes a maintained registry; Guardian static rule scans string literals near known state-key reads and flags off-label terms. Pair with `no-off-label-vocabulary` rule.

- **CDB-04** — Hero-section markup separation: net-worth tile lives INSIDE `.hero-section` (`index.html:906-909`, Drone A finding). Conditional-and-unconditional elements share a container, so the conditional coral hero gets visually softened by always-present green NW. Architectural fix: move always-present soft-state elements OUT of `.hero-section` so the section has only "the harsh number" job.

- **CDB-05** — `_skipBurnWarn` envelope flag (Drone D suggestion): mirror existing `_skipFreeMoneyGate` pattern. Cheap insurance for any future system-driven log path (cron auto-debit, push-handler, AI chat) to opt out of the Sub-bundle 2 warn-at-log-time interceptor without modifying the gate logic.

- **CDB-06** — Subagent redirect tooling slip (CC self-observation 2026-05-23): when redirecting an in-flight background agent, MUST use SendMessage to continue the agent — spawning a fresh Agent with redirect-only prompt creates a no-op agent with no original context. Lesson logged.

- **CDB-07** — `MEMORY.md` entry pending: add a memory pinning the staleness rule — "Numeric reproduction against tests/state-dump/*.json fixtures is illustrative only; never claim findings based on stale-fixture numeric reproduction." (Reinforces the existing [[feedback_dont_conflate_fixture_with_live_state]] memory.)

- **CDB-08** — Wrangler CF auth in CC sessions (2026-05-23): direct-KV pull failed `401 Unauthorized` from this env. Two structural options:
  - (a) Add `.env` entry `CLOUDFLARE_API_TOKEN=...` (with `Workers KV Read` scope, account-bound) for CC to attempt scoped reads autonomously per future session
  - (b) Document the `! npx wrangler login` flow on session-start so the auth happens in-band when needed
  Both reduce the "John types device token" friction for future fresh-state pulls.

- **CDB-09** — `BRAIN.aiContext.build()` substrate (Drone J finding): the chat system-prompt builders (`index.html:15277-15402` + `15561-15645`) raw-read S.bal · S.savingsBuckets · S.carloan · S.wrxValue · S.mumAccountBalance · S.superBalance · S.txns.filter(…). Two parallel raw-read builders ARE the FR-03 failure-mode root: the AI sees a non-canonical snapshot. One composed reader `BRAIN.aiContext.build()` calling `BRAIN.balance.get / BRAIN.assets.* / BRAIN.savings.getBuckets / BRAIN.summary.* / BRAIN.debts.getActive*` collapses both builders and structurally closes FR-03 at the read side. Single-largest leverage point per Drone J.

- **CDB-10** — Missing canonical readers to land alongside Drone J / F structural fixes:
  - `BRAIN.debts.getActiveSorted()` — ≥6 render sites reinvent `filter !paid + sort by priority`
  - `BRAIN.debts.getDueBeforeStrict(payDateMs)` — strict `<` for hero "safe before payday" (vs existing `<=`)
  - `BRAIN.debts.getActiveTotal()` — convenience aggregate
  - `BRAIN.savings.getTotalSaved()` — replaces ~6 inline bucket-sum reductions
  - `BRAIN.savings.getRainyDayBucket()` — replaces fuzzy-string lookups
  - `BRAIN.summary.discretionaryByCategory(fromMs, toMs)` — replaces ~6 inline txn-filter scans
  - `BRAIN.summary.recent(n)` — replaces slice/reverse boilerplate
  - `BRAIN.plan.getSnapshot()` extension — add per-item `ticked` + `override` so `renderPaydayBills/Debts/Savings/Upcoming` stop raw-reading `S.activePlan.ticks.*` / `.overrides` despite already fetching snap

- **CDB-11** — Guardian rule `no-raw-S-read-in-render` (Drone J + I synergy): static AST walker scopes to render fn / AI prompt builders / export serializers; forbids raw `S.{bal,savingsBuckets,debts,activePlan,txns,mumAccountBalance,superBalance,carloan,carloanOriginal,wrxValue}` reads; allow-list for canonical readers. Pairs with CDB-03's vocabulary rule.

- **CDB-12** — Renumbering proposal: INV-30 is taken (FX fee). The 3 new invariants this scoping surfaces need distinct numbers:
  - INV-32 — heal-on-read at every persistence boundary (Drone H)
  - INV-33 — render readers route through canonical bubbles (Drone J)
  - INV-34 — chat-action canonical dispatch (Drone F — every chat-action case routes through `BRAIN.chat.dispatchAction`)
  Sequencing matters: INV-32 protects the bottom of the stack, INV-33 protects the read perimeter, INV-34 protects the write perimeter against AI chat. Together they close most of recon Themes F/H/J.

- **CDB-13** — Inline HTML handler raw-read leak (Drone J finding): `index.html:1773, 1776` have `onclick="markDebtCovered(S.debts[activeDebtIdx]?.id)"` — a JS-scope guardian rule won't catch HTML-template raw-reads. Two options: (a) expand the guardian to also walk inline `onclick=` attributes, OR (b) ban inline event handlers in markup entirely (move to `addEventListener` in `init`), the cleaner architectural fix.

- **CDB-14** — 7 new INV-NN candidates from Drone K conservation audit (numbered to avoid collision):
  - INV-33 Hero-MAX coherence: `safeToSpendToday × daysRemaining + billsStillDue + provisions ≤ S.bal + ε`
  - INV-34 Receipt-is-hero: parsed receipt math reconciles to displayed hero within $0.50
  - INV-35 TO RECOVER lever displayed=computed: three levers must share the same arithmetic semantics
  - INV-36 Cross-tile payday day count: strip-payday = snap.daysRemaining = hero-ctx label (closes FR-06)
  - INV-37 NW single producer: strip-networth = nw-val = BRAIN.assets.liquidNet = calculateNetWorth().liquidNet
  - INV-38 Burn-7d floor source: `getBurn7d().floor === snap.dailyLiving.floor`
  - INV-39 Run-dry uses obligated-cash base, not raw S.bal

- **CDB-15** — 8 conservation breaks at dashboard render level (Drone K). Of particular note:
  - Break #1 — MAX PER DAY uses a DIFFERENT formula path from the hero (pre-payday-window bills filter + ignores livingRemaining/provisions/buffer + user-cap clamp + todaySpent subtract). Cannot multiply by daysRemaining to reach hero. Fix: re-base on `snap.derived.maxAffordablePerDay` or compute headroom/daysRemaining; ship with smoke for INV-33.
  - Break #2 — `cashflow-failure-line` runs-dry date uses raw `S.bal`, "over by" uses obligated-cash headroom. **Real money-truth bug.** One-line fix: replace `S.bal` with `S.bal − snap.bills.unpaidTotal` in the numerator.
  - Break #5 — strip-networth's `_m.totalNet` fallback is dead code that could silently render super-inclusive NW pre-PLAN-boot.
  - Break #6 — TO RECOVER category lever shows `_saved` (delta), floor levers show `_result` (absolute) — same block, two visual conventions. Cosmetic but reads as broken arithmetic to user.

- **CDB-16** — Label-truth concern: bills-correction reveal copy ("$5,684 → $219 real") describes the wrong field. `$5,684` comes from `snap.bills.total` (full bills sum, never was the unpaid claim); the "phantom" was the PRE-fix `unpaidTotal` value. Either fix the copy or change the reveal to compare the pre-fix vs post-fix unpaidTotal explicitly. Sub-bundle 1 shipped this — worth a hotfix.

- **CDB-17** — Smoke-spec strength: many existing specs check shape/presence only, not arithmetic (e.g. `cashflow-truth-reconciliation.smoke.js` Case 11 asserts "bills" and "living" strings present but not the math). Drone K identified 10 such gaps. Pattern: every cashflow-truth assertion should parse rendered HTML and reconcile arithmetically, not pattern-match text.

- **CDB-18 — PWA cache disease (ELEVATED to P0-4 per John 2026-05-23).** The "+Add Debt duplicate" mirage IS the symptom: John's phone is serving a pre-Sub-bundle-1 cached `index.html`. Three drones confirmed this independently:
  - Drone B — duplicate button NOT in HEAD source (single static markup at L956)
  - Drone H — `sw.js` has zero `cache.put/add/addAll` calls; install-time precache is empty; runtime cache never written
  - Recon Theme J / D5 F1 — already flagged but ranked low; John's lived experience promotes it
  **Why this is a P0-tier bug:** it masks every other bug. Every time CC investigates a phone-reported issue that's NOT in HEAD, the answer is "stale cache." Wasted investigation cycles + degraded trust + John's mental model of "which bugs are real" gets corrupted. Compounding cost.
  **Fix shape (small):** `sw.js` Install handler precaches `[index.html, manifest.json, sw.js, /icons/icon-192.png, /icons/icon-512.png]` keyed by `CACHE_VERSION` const; fetch handler does cache-first with network-fallback OR network-first with cache-fallback (TBD by use-case); `activate` event prunes prior versions; bump `CACHE_VERSION` on every deploy. Client-side: subscribe to `controllerchange` and toast "app updated — refresh".
  **Severity in SDD:** P0-4 alongside P0-1/2/3. Ship in the immediate hotfix bundle BEFORE Sub-bundle 2, because every Sub-bundle 2 finding will be "not in your build yet" until cache is reliable.

- **CDB-19** — Five divergent surplus formulas (redirect-K BREAK-1 generalization). User-facing "free money" has 5 independent readers (A surplus · B freeTotal · C safeToSpendHeadroom · D getGenuineSurplus · E BRAIN.summary.surplus().leftOver) with no algebraic relationship. Code comment at L21398-21407 claims they reconcile — STALE. AI chat reads D as "canonical"; canvas reads A/B; hero reads C; AI prompts also read E. Structural fix: declare ONE canonical surplus (recommend C `safeToSpendHeadroom` since it's the cash-truth one), deprecate the other 4, route all consumers through it. Single biggest "cross-surface inconsistency built-in" finding.

- **CDB-20** — Four divergent non-discretionary filter sets (redirect-K BREAK-2). `_NON_SPEND_CATS` (canonical F) vs `getMinDailySpend` (G) vs survival-cuts (H) vs `getBurn7d` (I). Car Loan inclusion differs across them — so the 7-day burn card and the spending-alert pace number disagree by any Car Loan txn in the 7-day window. Phase G Pass 1.a started the migration; G/H/I aren't on its list. Should be folded into the same campaign.

- **CDB-21** — Net-worth zero-amount-debt divergence (redirect-K BREAK-3). `calculateNetWorth` uses `!d.paid && !d.viaRent && d.amt > 0`; `BRAIN.assets.totalLiabilities` uses `!d.paid && !d.viaRent`. Same conceptual liabilities sum, two filters, divergent on zero-amount rows. Pair with INV-37.

- **CDB-22** — Boot-timing race for bill-paid detection (redirect-K BREAK-7). `BRAIN.bills.isPaidInCycle` (Sub-bundle 1 canonical) initializes mid-load. Pre-init `getBillsDue()` falls back to legacy `isThisMonthlyBillPaid` (flag-only). Same question, two answers depending on call timing during boot. Boot self-test should assert `BRAIN.bills` ready BEFORE any `getBillsDue()` consumer fires, OR `getBillsDue()` should hard-fail when `BRAIN.bills` not yet ready (no silent fallback).

- **CDB-23** — AI sees a surplus number the user can't see (redirect-K G8). `BRAIN.summary.aiContext().surplusWaterfall` exposes formula E to AI prompts. User dashboard renders A/B/C. AI advises against numbers the user never sees on screen. Compounds FR-03 from the read side. Closes when CDB-19 collapses the 5 formulas to 1 AND `BRAIN.aiContext.build()` (CDB-09) routes AI through canonical readers only.

- **CDB-24 — P0-5 isPaidInCycle Path 1 missing billDate guard (Drone L Finding 1).** `index.html:25457-25460`: Path 1 returns true on any `paidBills[key]` existence — no time check. The May 19 `processAutoDebits` writes flags for bills with future `billDate`. Money-truth bug: hero overstates available cash by the future-flagged total. Fix: require `billDate <= now OR txn-match` (Path 2 already does). One-line guard + new smoke. **P0-class, tonight-tier.**

- **CDB-25 — P0-6 getBurn7d NON_DISC list incomplete (Drone L Finding 2).** `index.html:24279`: 7 excluded cats but bills paid via quickLog can carry `Insurance/Streaming/Fixed/BNPL/Health` etc. Bills count as burn → inflated burn → wrong runs-dry date. Two fix shapes considered: expand NON_DISC vs make `markPaid` always write `cat:'Bills'`. Recommend the latter (cleaner — every paid-bill txn becomes Bills-categorised at source, every downstream filter benefits without per-filter sweep). **P0-class, tonight-tier.**

- **CDB-26 — Half-landed substrate (cross-cutting pattern).** Sub-bundle 1 propagated cashflow-truth to hero / receipt / TO RECOVER / burn card / bills-correction. It did NOT propagate to:
  - `renderAlerts` (legacy `safe = liveBal − dueTotal − immDebtsBeforePayday + bonusTotal`, no livingRemaining knowledge)
  - `getThisWeekProjection` pace (legacy `isThisMonthlyBillPaid`, flag-only)
  - `getBurn7d` NON_DISC filter (CDB-25)
  - "Daily living" tile (uses `S.activePlan.dailyLivingFloor`, not `S.weekdayBudget` — actually a DIFFERENT store; redirect-L #9)
  - 5 explanation-modal copy strings (redirect-L #1, #2, #3, #5)
  - Dashboard Recent Spending filter (only excludes roundups; Drone L Finding 8)
  - `renderMonthlyPosition`'s "Genuine surplus" label (redirect-L #3)
  Bundle proposal: **Sub-bundle 1.5 — cashflow-truth substrate completion.** Mechanical propagation, smoke-gated. ~6-10 commits depending on John's appetite.

- **CDB-27 — Bonus $1,341 invisibility (Drone L Finding 5, 10).** `S.activePlan.income.bonus = {amount:1341, status:'confirmed', included:false}` exists in state. Dashboard never surfaces it. TO RECOVER block lists three pain-levers (hold $25/day, hold $20/day, no takeaway 7d) while a one-tap toggle that adds $1,341 to the runway is invisible. Fix: TO RECOVER `bonusLever` row at top when status=='confirmed' && !included. Label "💰 Include $1,341 bonus → +$1,265". Cheap money first.

- **CDB-28 — Three different bill-paid readers in same card (Drone L Finding 9).** `#max-day-card` shows MAX PER DAY (reads `getBillsDue`/`isThisMonthlyBillPaid`) + Pace (reads `isThisMonthlyBillPaid`); coral hero ABOVE reads `BRAIN.bills.isPaidInCycle`. User sees inconsistent numbers from inconsistent substrates. Fix: route `getThisWeekProjection` and `getDynamicDailyBudget` through `BRAIN.bills.isPaidInCycle`. Same canonical reader as hero.

- **CDB-29 — `explainMaxPerDay` modal contradicts itself (redirect-L #4).** Inside one modal: deduction line uses `getActiveDebtsDueBeforePayday()` (filters by `delayDate`), breakdown list filters by `d.day || d.dueDay`. Different fields, same modal. Debts can be deducted but not listed (or vice versa). One-modal arithmetic doesn't reconcile.

- **CDB-30 — Two separate "Daily living" stores never synced (redirect-L #9).** `S.weekdayBudget`/`S.weekendBudget` (Settings, default 60/180) vs `S.activePlan.dailyLivingFloor` (PLAN, default 30). Both labelled "daily living" in UI; editing one doesn't propagate to the other. This is the structural cause of Drone K Break #1 / CDB-19's surplus formula divergence. Fix: collapse to one store with documented role; UI labels distinguish "Spending cap" vs "Survival floor" (per CDB-03's vocabulary registry).

- **CDB-31 — Dead-end modals (redirect-L #6, recon Theme E confirmation).** `explainNegativeProjection` L12593, `openPaydayLockPlan` L12613, `openPaydayAutoAllocate` L12428 — all show options as info text, no action buttons. Sub-bundle 1's TO RECOVER pattern is the model for how to fix these.

- **CDB-32 — Bills-correction copy attribution wrong (Drone L Finding 6).** `index.html:7066-7068`: "$5,465 of bills detected paid via bank match" attributes 100% to Path 2 (txn-match), but most hits this cycle are Path 1 (`paidBills` flag — auto-debit or manual). Misleading trust moment. Fix: count Path 1 vs Path 2 separately, rephrase honestly ("X auto-debited or manually marked; Y detected via bank match").

- **CDB-33 — Tiered build pipeline (John's meta-proposal 2026-05-23).** Formalise the pattern that emerged this session:
  1. **Gather** — parallel investigation drones (read-only)
  2. **Analyse** — collector synthesises into safe-now vs takes-longer
  3. **Easy fixes (mechanical, file:line known)** — parallel build agents with implementation manuals; Sentinel-gates each commit. Examples: toISOString 9-site sweep · render-side raw-S read migration · vocabulary registry sweep.
  4. **Bigger changes (substrate-shaping)** — council review: Guardian 4-layer + INV touched/added + FEATURE-MAP impact + priority justification. Examples: BRAIN.aiContext.build · BRAIN.chat.dispatchAction · heal-on-read INV-32 · PWA cache strategy.
  ADR-H proposed: "Tiered build pipeline" — codifies the council, the manual format, and the Sentinel-gate contract per tier. Lift this from one-session improvisation to durable process. Captures lessons: the auditor catching recon over-claims (Drone C found "1+1 not 4"), drone-pairs cross-validating each other (K Break #2 ↔ L Finding 12), the redirect-K agent producing complementary findings beyond original K — all things a formal pipeline can reuse and amplify.

- **CDB-34 — STANDING RULE: Ledger Walk is Step 0 of every session (John 2026-05-23, in response to P0-5 misdiagnosis).** Drone L diagnosed Allianz CTP + KIA Rego as "phantom flags" from snapshot inspection alone — saw flag present, saw billDate future, concluded bug. Did NOT walk the ledger. Both flags were legitimate early-payment markers backed by debit txns 5-6 days prior. **The pipeline was about to ship a "fix" that would have lied to John by $1,028 in the catastrophic direction.**
  
  **New rule (codify in CC-PRINCIPAL-ENGINEER-MANUAL.md):** Before ANY state-derived finding can be promoted to bug status, walk the txn ledger and prove the claim reconciles to source events. Flags are derived convenience; txns are ground truth.
  - paid:true + matching debit txn → LEGITIMATE (regardless of billDate — early payment is valid)
  - paid:true + NO matching debit txn → SUSPECT (true phantom-flag bug)
  - paid:false → unpaid
  
  **Pipeline correction — 6 tiers, Ledger Walk is Step 0:**
  ```
  0. Ledger Walk     — reconstruct state from txn events; prove flags/derived
                       reconcile; surface ledger-orphans and ledger-extras
  1. Gather          — drones investigate WITH ledger-walk evidence per claim
                       (state-derived claims rejected without ledger backing)
  2. Analyse         — collector: safe-now vs needs-review
  3. Conformance     — architectural-fit verification + DIAGNOSTIC-VALIDITY:
                       did the drone walk the ledger for state-derived claims?
                       Bidirectional (back to drone if not walked).
  4. Build           — parallel build-agents with manuals, Sentinel-per-commit
  5. Council         — push verdict
  ```
  
  Ledger-walk script lives at `scripts/recon/ledger-walk-paidbills.js` — generalize to walk every state-derived claim. Conformance tier now adds the question: "is this finding ledger-backed or snapshot-inferred?"

- **CDB-35 — P0-5 RE-DIAGNOSED (txn-anchored, not date-anchored).** Original P0-5 was wrong. Real bug surface from ledger walk:
  - **Stan-7** is the only genuine ledger-orphan flag (bare `true`, no `_txnTs`, no matching txn). Magnitude ~$13. P3, not P0.
  - **YouTube Premium-18** is AMBIGUOUS — flag matches TWO $16.99 txns in cycle (May 11 + May 18). Possible duplicate charge worth manual check.
  - **Correct fix shape:** `isPaidInCycle` Path 1 requires `_txnTs` OR a matching debit txn in window. If flag is bare `true` AND no matching txn → SUSPECT-treat-as-unpaid. Date-anchored guard rejected (would have broken legitimate early payment).

- **CDB-36 — P0-6 RE-INVESTIGATION NEEDED.** Ledger walk shows `markPaid` writes the bill's TAG (Fixed/Streaming/Insurance/BNPL/Loan/Bills) as cat — variants confirmed. BUT some of those tags are discretionary-recurring (Netflix, YouTube, Claude.AI subscriptions) which John MAY want counted as burn. "Always write cat:'Bills'" over-corrects. Real question is "truly fixed vs discretionary-recurring" — needs values call + data-shape decision before fix shape is finalised. **P0-6 demoted from tonight-tier** pending re-scope.

- **CDB-37 — Whole slate held pending re-verification.** Of the 9 commits scoped tonight, ledger walk affects:
  - P0-1, P0-2, P0-3a, Break #2 numerator, Net-worth Option C, TO RECOVER bonusLever, CDB-28 reader swap — all CODE-STRUCTURAL (no state inference). **Stand pending sanity re-check** that no implicit state assumption hides.
  - P0-5 — RE-SCOPED (txn-anchored fix, demoted to P3 Stan-7 cleanup).
  - P0-6 — HELD pending values call.
  Conservative re-tally: **7 commits ship-ready** after sanity recheck; **2 commits re-scope first**.

- **CDB-38 — Bonus is in S.bal already (not in a bucket).** Ledger walk 2026-05-23 confirmed: salary credit 2026-05-14T06:40 was $8,623.33 with note "PAYSLIP with Bonus" — that's declared monthly S.income $7,282 + bonus $1,341 folded together. No dedicated bonus bucket exists. Current S.bal $781.66 includes the bonus AND reflects all post-payday spending. **`bonus.included:false` means "don't add to forward-allocation plan math" (correct — already arrived), NOT "available cash to add to current headroom"**. Any UI surface that interprets `!included` as "addable" is double-counting against S.bal.

- **CDB-39 — bonusLever (CDB-27) RESOLVED: removed entirely.** Built per Drone L Findings 5/10 + Drone M mockup; passed technical Council; FLAG-WITH-CITATION assigned, ack received, push QUEUED. John caught mid-bundle: the lever's "Include $1,341 → +$Y headroom" was adding the bonus to a basis that already contained it. Same disease class as the backwards P0-5 (semantic misinterpretation). Reverted in working tree before push. **John 2026-05-23: option (a) — remove entirely. Honest. The bonus is in S.bal; `included:false` is allocation-math gating; nothing the dashboard can legitimately offer as "+headroom" without an architectural change.** Bonus visibility question deferred to a real solution (dedicated bucket or post-payday spend-down tracking) or Bundle 33 UI redesign. Drone L Findings 5/10 stay open as observations; no current implementation answers them.

- **CDB-40 — ADR-H Human Verdict refinement: FLAG-WITH-CITATION must cite SPECIFIC interpretation, not adjacent observation.** The bonusLever catch surfaced a gap in the Human Verdict block. The original FLAG-WITH-CITATION cited Drone L Finding 5 ("bonus is invisible") as policy — but that was an OBSERVATION about the surface, not a RATIFICATION of the specific interpretation the lever encoded (treating `!included` as "available to add"). The block should HALT when an interpretation hasn't been explicitly ratified by John, even when an adjacent observation is on record. Proposed amendment to ADR-H Decision-gate spec:
  > **Citation discipline**: a FLAG-WITH-CITATION verdict requires John's prior ratification of THIS SPECIFIC INTERPRETATION (or pattern that this commit propagates). An adjacent observation about the surface (e.g., "this is invisible", "this needs work") does NOT satisfy the gate — the gate asks "did John pick the policy this implementation encodes?" not "did John flag this surface?" Distinguish:
  > - Observation: "the bonus is invisible on the dashboard" — does NOT ratify any specific lever implementation
  > - Policy: "the bonus, when confirmed-but-excluded, should appear as a one-tap recovery option in TO RECOVER" — would ratify the lever shape
  > - Implementation interpretation: "the lever adds `bonus.amount` to `_heroNumber`" — needs explicit ratification of THIS arithmetic, separate from policy
  >
  > When in doubt → HALT, not FLAG-WITH-CITATION. The friction is cheaper than the misdiagnosis.

- **CDB-41 — `scripts/recon/ledger-walk-bonus.js` is the third sibling ledger-walk script** (after paidBills/debts/buckets/cycle-totals). Pattern: domain-specific walks for any specific question. The bonus walk surfaced the double-count in ~30 seconds. Generalize the pattern — when a Human Verdict on a state-derived UI shows FLAG-WITH-CITATION, run a quick domain walk to verify the citation actually backs the implementation BEFORE granting the verdict.

(append as drones return)

---

## Open at session start

- Sub-bundle 2 (warn-at-log-time) — next per locked sequence
- Sub-bundle 3 (Theme B + F canonical-writer bypass closure) — likely absorbs flagged items 2 + 3
- P0-1/P0-2/P0-3 status pending Drone E
- Themes H-M (chat-action bypass, boundary-day, lost-state heal, toISOString, deployed-vs-committed, doc drift)
- Bundle 23 cloud sync · Phase B encryption · Bundle 33 redesign — all post-substrate-clean

---

## Step 2 EXPANDED (per John 2026-05-23 — "scope out the project, employ the team")

**Tier-1 — 13 drones in flight (5 original + 8 new, parallel):**

Original (running / completed):
- ✅ A — net-worth render path (Single static site L906-909, formula L3793, Option matrix produced)
- ✅ B — duplicate +Add Debt (NOT in HEAD source; cache/extension/missed-path investigation needed)
- 🛰 C — MAX PER DAY card
- 🛰 D — Sub-bundle 2 log-time interceptor scoping
- 🛰 E — P0-1/P0-2/P0-3 verification at HEAD

Expanded (just launched):
- 🛰 F — Chat-action canonical bypass deep audit (REC-1: ~20 actions in executeChatAction enumerated)
- 🛰 G — `toISOString().slice(0,10)` grep + classify (REC-2: Theme K, P0-3 siblings)
- 🛰 H — Theme J heal-on-read audit (every persistence boundary — propose INV-30 substrate)
- 🛰 I — Theme D vocabulary registry build (Buffer / Locked / $X/day / Covered / Cleared)
- 🛰 J — Render-side raw-S read enumeration (DARK REGION, recon REC-5 candidate)
- 🛰 K — Conservation invariant audit (every dashboard derived number, can it close to a conservation equation?)
- 🛰 L — Dashboard "doesn't compute" walk (cross-tile arithmetic reconciliation against canonical readers + live oracle)
- 🛰 M — ASCII mockups: net-worth options · MAX PER DAY rework · Sub-bundle 2 interceptor · duplicate-button states

**Tier-2 Auditors** (queued, run after Tier-1):
- A1: verifies code drones (A,B,C,E,F,G,J,K)
- A2: verifies surface drones (D,H,I,L)
- A3: cross-checks design (M) against code findings

**Tier-3 Specialists** (queued):
- Security Advisor — Phase A post-deploy posture, Phase B gap surface, race windows, audit-log discipline
- Design Lead — net-worth · MAX PER DAY · Sub-bundle 2 interceptor surface · loop Opus material

**Tier-4 Final Decision + SDD draft:**
- Cross-reference all findings, identify cracks, produce `docs/sdd/SDD-2026-05-22-cashflow-truth-sub-bundle-2-and-flagged-fixes.md`. Bundle scoping, P0 sequencing, invariants impacted, smoke specs needed, values calls for John.

---

## SESSION CLOSE-OUT — append 2026-05-23 (the late-night arc this file was missing)

The file above stops at ~19:52 with Tier-2 auditors queued. From 19:52 → 22:34 a SUBSTANTIAL arc happened that's the most consequential of the whole session. Capturing it here so future sessions have the full picture, not just the part that was being written live.

### Final commit ledger (15 commits tonight, in order)

| # | Commit | What |
|---|---|---|
| 1 | `1307f08` | P0-1 payBillNow double-debit (one-line delete) |
| 2 | `b9a8938` | P0-2 knownUpcoming filter |
| 3 | `fd69cc5` | P0-3a toLocalISODate Sydney TZ off-by-one |
| 4 | `f73ec41` | Break #2 runs-dry obligated-cash basis |
| 5 | `622c76b` | Net-worth Option C (conditional hide on coral) |
| 6 | `a3165bf` | CDB-28 pace surface canonical reader |
| 7 | `e613b92` | docs+tooling: bonus walk + CDB-38..41 |
| 8 | `b4cedfc` | docs(ADR-H): citation discipline refinement (after bonusLever catch) |
| 9 | `c6b1d3c` | P0-5 isPaidInCycle txn-anchored guard |
| 10 | `7a1d5a8` | **fix(security): apiKey leak hotfix** + NEVER_SYNC redaction + 500kB cap |
| 11 | `ee2d6c4` | docs(SDD): Bundle 23 10-drone synthesis (v1/v2 split version) |
| 12 | `ccb3039` | docs(SDD): v2 paused pending Q4 |
| 13 | `68c47c4` | **docs: Bundle 23 RE-SCOPED to single-device sync; Phase B committed-next** |
| 14 | `0453719` | **docs: install standing development pipeline as permanent OS** |
| 15 | (this commit) | session close-out + push-reliability finding + Android platform pin + OPEN-BUGS append |

### The major arcs after 19:52

**1. apiKey leak surfaced + patched (commit `7a1d5a8`)**
- Drone R5 (Bundle 23 Tier-1 Red Team) finding T6: `PUSH.pushFullState` had been sending raw localStorage including `S.apiKey` (Anthropic sk-ant-*) to worker-KV plaintext since Bundle 32a shipped (2026-05-20)
- Fix: `NEVER_SYNC` deny-list (`['apiKey', '_prevState', 'chatHistory', 'pin', 'pinHash']`) mirroring `buildFullExport`'s existing pattern; 500kB body cap
- Followup action (John, completed): Anthropic key rotated at console.anthropic.com. Old key revoked. New key entered console → Settings, his hand only. **Never touched CC or repo.**
- Verified live 2026-05-23T22:25 AEST: fresh KV pull shows NO `apiKey` field in pushed blob; bal $714.67, 241 txns visible
- SECURITY.md decision-log entry added

**2. Bundle 23 re-scoped to single-device sync (commit `68c47c4`)**
- 10 drones (5 Gather CS-A..E + 5 Red Team R1..R5) scoped a v1/v2 split that assumed multi-device merge complexity
- John's Q4 answer 2026-05-23: phone PWA only, single device, one writer → no concurrent-write problem → no merge machinery needed
- All v2-specific complexity (Lamport clocks, per-field merge, intent-keyed dedup, conflict modal, `_device`+`_localId`, tombstones, server-side merge, `_remote:true` tagging) EVAPORATED for single-device scope
- 8-commit slate locked: freshness anchor + worker meta + BRAIN.cloudSync bubble + pullOnOpen orchestrator + boot wiring + cloudSyncEnabled rename + sync UX + BroadcastChannel multi-tab safety
- v1 build attempted mid-session (commit 1 of 11 schema-versioning substrate) then REVERTED unpushed pending Q4
- SDD rewritten end-to-end to reflect single-device-sync architecture
- See `docs/sdd/SDD-bundle-23-cloud-sync.md` for the locked spec

**3. Phase B rescued from "deferred indefinitely" to "committed-next" (in SECURITY.md decision-log)**
- Initial framing during v2 scoping: "v2 MUST NOT ship before Phase B encryption-at-rest" (hard dependency)
- After single-device re-scope: the multi-device exposure amplification argument no longer binds
- John's call: Phase B still needed for plaintext-already-in-KV (regardless of sync) → sync ships first on plaintext-KV knowingly, Phase B is the IMMEDIATE next bundle
- `SECURITY.md` 2026-05-23 decision-log entry CONTRACTS this ordering: future-CC reading SECURITY.md is bound to ship Phase B immediately after sync

**4. Standing pipeline installed as permanent OS (commit `0453719`)**
- `docs/PIPELINE.md` — 12kB standing pipeline doc (6 tiers, standing rules, session bring-up + close protocols, WHY-each-tier-exists with the specific bug each tier was born to stop)
- `CLAUDE.md` MANDATORY SESSION-START READ banner at top; PIPELINE.md as read-order #1
- `CC-PRINCIPAL-ENGINEER-MANUAL.md` top banner + §0 amended; pipeline outranks the manual when conflicting
- `MEMORY.md` pinned `pipeline_session_start.md` at TOP with 🛡️ marker
- Future-CC opening any session-start path lands on `docs/PIPELINE.md` first

**5. Push reliability bug confirmed LIVE in production (this commit)**
- John used the app, asked for a state verification
- Fresh KV pull showed apiKey STILL present + no new txns despite his usage
- Investigation: `state-full-meta.lastPushedAt` = 2026-05-23T07:17:57.959Z (17:17 AEST)
- That's HOURS before the apiKey hotfix shipped (20:41 AEST) + before the last ~10 commits
- John's phone localStorage had advanced: bal $714.67, 241 txns (vs KV's $781.66 / 231 txns)
- **~5 hours of activity sat unpushed.** The 30s debounce timer never fired during foregrounded use; the pagehide flush silently dropped because keepalive body exceeded 64KB (state blob is ~292KB pre-redaction, ~139KB post-redaction; both >> 64KB)
- John deleted PWA + reinstalled (Android Chrome PWA on S23 Ultra) → forced fresh SW + fresh `index.html` → logged $0.10 test → kept tab open 40s → 30s debounce fired (regular fetch, NO keepalive limit) → push delivered → fresh pull verified: apiKey absent, bal $714.67, 241 txns, $0.10 Test txn visible
- **THREE diagnostic axes verified simultaneously:** (1) NEVER_SYNC redaction works on real push, (2) data delivered, (3) new code loaded post-reinstall
- Push reliability bug is the FOUNDATIONAL fix gating Bundle 23 sync. Filed as OPEN-BUGS.md #50 (URGENT). Re-ordered roadmap: push-reliability fix FIRST, then Bundle 33-cache, then Bundle 23 sync, then Phase B.

**6. John's actual platform clarified: ANDROID, not iOS**
- Tonight I wrote "iOS Safari" multiple times before John corrected: he uses **Android Chrome PWA on Samsung Galaxy S23 Ultra**, full Android ecosystem
- Scrubbed iOS references from `docs/PIPELINE.md` (line 106) and `docs/sdd/SDD-bundle-23-cloud-sync.md` (line 261)
- Added platform context to `CLAUDE.md` header (Android Chrome PWA on S23 Ultra, NOT iOS Safari)
- Memory pin created: `john_environment_android_s23_pwa.md` — load-bearing context for future-CC scoping any phone-behavior assumption

### Updated roadmap (locked post-tonight)

1. **Push reliability fix** (URGENT, OPEN-BUGS #50) — foundational. Without it, every backgrounding of John's PWA potentially loses queued state. Design options on file (drop keepalive + shorter debounce + gzip belt-and-suspenders). ~2-3hr build.
2. **Bundle 33-cache** — sw.js precache + `CACHE_VERSION` + skipWaiting + controllerchange. Per Drone H. ~1-1.5hr. Closes the cache disease (PWA-reinstall workaround replaced by automatic propagation).
3. **Bundle 23 single-device sync** — per locked SDD, 8 commits, ~2hr + phone-verify. Now BUILDS ON working push reliability.
4. **Phase B encryption-at-rest** — committed next per SECURITY.md decision-log. Passphrase-derived key, AES-256-GCM, worker holds only ciphertext. ~4-6hr dedicated session.

### What was attempted and reverted (not lost, just held)

- Bundle 23 v1 commit 1 of 11 (schema-versioning substrate): edited, smoke-passed 3/3, then REVERTED before push when John's Q4 answer landed and re-scoped the bundle entirely. Regenerable from SDD §11 in ~5 minutes if/when v1 (now single-device sync) resumes.
- bonusLever (Drone L Finding 5+10, Drone M mockup) was built and locally committed-ready when John caught it as a DOUBLE-COUNT against the bonus already in S.bal. Reverted entirely (option a per CDB-39). The visibility-of-bonus question stays open architecturally; the wrong implementation does not.

### Standing rules earned this session (and codified)

- **Ledger Walk Step 0** is mandatory before any state-derived finding promotes to bug (after the backwards-P0-5 catch). Codified in ADR-H + memory pin + PIPELINE.md.
- **Human Verdict block** (intent-drift gate) is required at Council time (after the bonusLever near-miss). Codified in ADR-H + PIPELINE.md.
- **Citation discipline** for FLAG-WITH-CITATION: must cite SPECIFIC implementation interpretation, not adjacent observation (after the bonusLever's "drone said bonus is invisible" → "lever adds bonus.amount" gap). Codified in ADR-H amendment + commit `b4cedfc`.
- **The PAUSE is a tier-level move.** Refusing to build when scope is wrong (v2 → single-device re-scope) is itself the highest-leverage act of a session. PIPELINE.md §WHY codifies this.
- **Push/delivery verification** is required: phone-verify that pushes actually land on John's device, not just green smoke (after tonight's 5-hour silent push gap). PIPELINE.md §WHY codifies this.

### Where future CC opens next session

Per the install at commit `0453719`:
1. Read `docs/PIPELINE.md` FIRST
2. Read this file (most recent session memory) — full session context lives here including all the late-night work above
3. Read OPEN-BUGS.md #50-55 — push reliability is foundational
4. Roadmap: push reliability fix → Bundle 33-cache → Bundle 23 sync → Phase B
5. John's platform: Android Chrome PWA on S23 Ultra. NOT iOS.
