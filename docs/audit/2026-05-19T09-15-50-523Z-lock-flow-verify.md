# Lock-flow visual verification — Bundle 32.7 Pass 1

**Date:** 2026-05-19T09:15:58.513Z
**Final verdict (hash-grounded):** ✓ **PASS**
**Hash equivalence:** ✓ unlocked frames byte-equal · locked frames byte-equal · unlocked ≠ locked
**Haiku verdict:** PASS (perStep failures: 0; verdict-vs-perStep internally consistent: true)
**Frames analyzed:** 7
**Model:** claude-haiku-4-5-20251001
**API trace:** `docs/audit/2026-05-19T09-15-50-523Z-lock-flow-verify.jsonl`
**Cost:** $0.0092 (in=4199 out=995)
**Latency:** 8.0s

## Summary

All seven steps show consistent lock/unlock state transitions with no cross-surface contradictions. Lock badge appearance/absence, banner text, and button states (RE-PLAN vs. absence) are synchronized at every step.

## Per-step verdict

### Step 1 — Boot, unlocked baseline (no lock badge visible)

- ✓ visible state: `unlocked`
- matches expected: true
- notes: No lock badge visible. Banner section absent. UI shows standard payday plan layout with 'ALLOCATING THE REMAINDER' section visible. Consistent with unlocked baseline.

### Step 2 — Locked via CANVAS_LOCK — canvas Lock save handler (lock badge should appear)

- ✓ visible state: `locked`
- matches expected: true
- notes: Lock badge clearly visible in tan/beige banner at top: 'Plan locked 19 May · 🔥 5 cycle streak'. Banner states 'Tick items as you handle them. Re-plan to edit allocations.' with 'RE-PLAN' button. Lock state is explicit and prominent.

### Step 3 — Unlocked via PLAN_UNLOCK_INLINE — inline banner Unlock (r77 path; lock badge should disappear)

- ✓ visible state: `unlocked`
- matches expected: true
- notes: Lock badge and banner have been removed. UI returns to standard layout. 'ALLOCATING THE REMAINDER' section visible again. Transition from step 2 lock banner to absence of banner confirms unlock via inline action.

### Step 4 — Locked via PLAN_LOCK_PAYDAY — legacy plan-mode flow (lock badge appears again)

- ✓ visible state: `locked`
- matches expected: true
- notes: Lock badge and tan banner reappear with identical messaging as step 2: 'Plan locked 19 May · 🔥 5 cycle streak'. Re-lock via legacy plan-mode flow is visually evident.

### Step 5 — Unlocked via CANVAS_UNLOCK — canvas Re-plan (lock badge clears)

- ✓ visible state: `unlocked`
- matches expected: true
- notes: Lock badge and banner cleared again. Canvas unlock via Re-plan removes all lock UI. Standard layout restored. Consistent with expected canvas unlock behavior.

### Step 6 — Re-locked via CANVAS_LOCK (lock badge appears)

- ✓ visible state: `locked`
- matches expected: true
- notes: Lock badge and tan banner reappear for second time with same 'Plan locked 19 May' message and 'RE-PLAN' button. CANVAS_LOCK re-engages lock state successfully.

### Step 7 — Final unlock via PLAN_UNLOCK_INLINE (badge clears at termination)

- ✓ visible state: `unlocked`
- matches expected: true
- notes: Lock badge and banner completely absent. Final unlock via PLAN_UNLOCK_INLINE removes all lock UI. Plan returns to fully unlocked state at termination.

## Transitions

- ✓ step 1 → 2: expected `unlocked→locked` · observed: unlocked→locked
- ✓ step 2 → 3: expected `locked→unlocked` · observed: locked→unlocked
- ✓ step 3 → 4: expected `unlocked→locked` · observed: unlocked→locked
- ✓ step 4 → 5: expected `locked→unlocked` · observed: locked→unlocked
- ✓ step 5 → 6: expected `unlocked→locked` · observed: unlocked→locked
- ✓ step 6 → 7: expected `locked→unlocked` · observed: locked→unlocked
