# Walk-and-Judge — AUDITOR verdict audit (2026-05-26)

**Auditor role:** second pair of eyes on the primary walker's 6 verdicts.
**Inputs:** `tests/walker-out/2026-05-26T07-29-18/walk.json` (lands + probes), per-flow screenshots, `index.html` source (grep + read at cited lines), `scripts/walker/run-walk.js` (to verify which affordance each step actually drove).
**Fixture:** `state-snapshot.fake.json` — FAKE persona "Sam", positive surplus, `S.bal=4800`, `pushOnSaveEnabled:false`, frozen 2026-05-26T10:00:00+10:00. **NOT John's real data.**
**Discipline:** Ledger Walk — audit-log lands / in-page probes are truth; a verdict may not promote a candidate to a confirmed bug unless the lands or probe actually show it.

---

## Verdict-audit table

| # | Flow | Primary verdict | AUDITOR ruling | Evidence cited | Over-claim caught / nuance |
|---|------|-----------------|----------------|----------------|----------------------------|
| 1 | darwin-A-quicklog | ✗ BROKEN (silent uncredited save, no picker) | **CONFIRM** | walk.json step-5 lands = `txn_record/log-expense` + `balance_apply_delta/log-expense` ONLY — **no `bucket_saved_change`**. before/after: bal 4800→4500, `bucket:Darwin Trip` 800→**800** (unchanged), txns 9→10. step-03 screenshot: Quick Log "Savings" type selected shows Amount / Description / Type / "Recurring — add to bills list" — **no bucket or goal picker exists**. step-06 aftermath: no warning surfaced; the $300 shows only as "−$300.00 · Savings" in the feed. | None. The verdict is precise: cash dropped, goal did not grow, no destination affordance, silent loss. Nuance worth keeping: "Savings" here = an *outflow expense tagged Savings* (`log-expense`), not a transfer — it leaves the cash but lands nowhere creditable. |
| 2 | darwin-B-plantick | ✓ WORKS | **CONFIRM** | walk.json step-6 lands = `txn_record` + `balance_apply_delta` + **`bucket_saved_change`** + **`plan_tick`** (all `plan-savings-tick`). before/after: bal 4800→4500, `bucket:Darwin Trip` 800→**1100**, txns 9→10. step-06 screenshot: "Darwin Trip $1,100 of $4,000 (28%)", "✓ allocated — tap to undo", $300 assigned. | None. The crediting machinery fires on the plan-tick path; same $300, goal grows. |
| 3 | log-transaction | ✓ WORKS — implies crediting works, so Darwin bug = missing PICKER not broken crediting | **CONFIRM** (incl. the implication) | walk.json step-5 lands = `log-expense` (txn+bal) + `roundup` (txn+bal+**`bucket_saved_change`**). before/after: bal 4800→4795 (−4.50 −0.50), txns 9→**11** (+2), `bucket:China Holiday` 1500→**1500.50**. | None. The implication holds: `bucket_saved_change` fires for the round-up → bucket-crediting works in general. Therefore darwin-A's failure is specifically the **absent destination picker on the Quick Log Savings path**, not a broken crediting writer. Verdicts 1+3 are mutually corroborating. |
| 4 | bills-mark-paid | ✗ CANDIDATE (undo no-op; "did the walker just drive the wrong affordance?") | **UPGRADE → CONFIRMED writer-key-vs-reader-key bug, affects BOTH undo paths** | **Pay:** step-2 lands = `txn_record/pay-bill-now` + `balance_apply_delta/pay-bill-now` + `bill_mark_paid/pay-bill-now` + `bill_mark_paid/auto-detect` + `notif_add`; bal 4800→4750, txns 9→10. **Undo:** step-4 lands=**[]**, bal stays 4750, still paid. **Key trace:** `payBillNow`→`BRAIN.bills.markPaid` (index.html:20085) builds key at :25192 `paidBillKey(name, day, resolvedMonth, resolvedYear)`; Phone Plan day 10 < payday 15 (fixture confirms both) → month bumps +1 (:25184-25187) → **writer key `2026-6-Phone Plan-10`**. Walker undo (run-walk.js:224) calls `paidBillKey('Phone Plan', 10)` (no month/yr) → today's month → **reader key `2026-5-Phone Plan-10`** → `unmark` returns `{ok:false,reason:'not-paid'}` (:25448) → no-op. | **I challenged this hard per the brief and it survives — and strengthens.** The real user-facing modal button is `undoBillPaid()` (onclick at index.html:1823, fn at :8809). It reads `paidBillKey(b.name, b.day)` at :8812 — **also no month/year → also `2026-5-Phone Plan-10`** → same mismatch. So the walker did NOT merely drive the wrong affordance; the genuine modal undo has the identical reader-key, so it would no-op too. This is a real cycle-relative key bug, not a test artifact. **Note: undo-by-key for *future-bumped* day<payday bills is the affected class; same-cycle/day≥payday bills wouldn't bump and would undo fine — scope is the day<payday cohort.** |
| 5 | plan-lock | ✓ WORKS (canonical path) | **CONFIRM canonical path clean; 2nd (legacy/UI) unlock path remains UNWALKED — surface NOT fully cleared** | step-4 probe (locked): `activePlan_lockedAt=1779753610101`, `brain_isLocked_getter=true`, `localStorage slyght_payday_plan` lockedAt set, `legacy_planLocked="<none>"`. step-6 probe (after `BRAIN.plan.unlock`): all null/false/cleared. Lock/unlock lands = `payday_plan_locked`/`payday_plan_unlocked` (canvas-lock). | **Correct to hold the line here.** The walker drove only the CANONICAL `BRAIN.plan.unlock`, which is clean. The memory'd 3-store divergence is about a SECOND (legacy/UI) unlock path that this walk did NOT exercise. Do **not** mark the whole lock surface clean — the divergence remains an **⊘ unwalked candidate**. (Aside: `legacy_planLocked` was already `<none>` even when locked, i.e. the legacy bool isn't written by the canonical lock — consistent with divergence living on a different path.) |
| 6 | ai-provenance | ✗ BROKEN (#1 finding) | **CONFIRM all 5 sub-claims; ENFORCE the raw-balance nuance** | `buildSystemPrompt` (:15332) is the CORRECT builder — uses `getLiveBal()` (:15335), `getGenuineSurplus()` (:15340), disclaimer (:15375). **Grep confirms 1 occurrence total = DEAD CODE, 0 callers.** Live prompt is the inline `systemPrompt` in `sendChatMessage` (:15665), sent to the API at `system: systemPrompt` (:15766). Verified line-by-line: (a) **spend-power**: :15676 `can spend $${getDynamicDailyBudget()}` — probe `getDynamicDailyBudget=60` vs `getGenuineSurplus=0` → DIVERGES; (b) **id miss**: :15649 `goals.find(g=>g.id==='rainy-day-fund')`; probe `goal_ids_present=['rainy-day']`, `rainy_lookup_byPromptId=false` → undefined → :15692 hardcoded `'9,000'` fallback; (c) **date contradiction**: :15704 literal "Darwin trip: June 7-15" vs probe intent {Aug 1–10}; (d) **dead disclaimer**: no disclaimer text in :15665-15743; only one lives in dead `buildSystemPrompt` (:15375); (e) **raw read**: :15672 `BALANCE: $${S.bal.toFixed(2)}` — RAW `S.bal`, not `getLiveBal()`. Hardcoded contradictions confirmed: $50,000 deposit target (:15691) vs "$3k currently" (:15715); Salary $117,500 (:15698); $68k total (:15740); KIA/Firstmac by name (:15693). | **Nuance enforced (do not overstate):** on THIS fixture `S.bal=4800` and `getLiveBal=4800` → `bal_divergence=0`. So the raw read is **not** a live wrong-number here; it is a **structural risk** (any state where derived≠raw would silently feed the wrong number), not a demonstrated wrong number on this walk. The live, demonstrated bites are (a) spend-power 0-vs-60, (b) rainy-day-fund id miss → hardcoded $9k, (c) Darwin date contradiction Aug-vs-June, (d) absent disclaimer, plus (e) the raw-read as a latent structural risk. Verdict stands; the framing must not claim the balance was wrong on this fixture. |

---

## Cross-cutting notes (confirmed as benign / canary)

- **15 pageErrors = boot TDZ "Cannot access 'BRAIN' before initialization" at `isPaid` index.html:4512.** Confirmed **boot-only**: `getBillsDue`'s inner `isPaid` (:4511-4517) guards `typeof BRAIN !== 'undefined' && BRAIN.bills && ...` and falls back to `isThisMonthlyBillPaid` (:4517) — i.e. a designed stub fallback during the pre-init window. Proof it heals post-boot: every later probe returned real values (`getLiveBal=4800`, `getGenuineSurplus=0`, lock getters live, bucket writers fired). Same class hits `getLiveBal` (:3644) via `getGenuineSurplus` during early `autoSnapshot`. **Not a runtime functional bug; boot-order noise. Worth a cleanup ticket, not a P0.**
- **Boot self-test "1 failure: [push-on-save default: enabled unless explicitly off]".** Confirmed **canary working as designed**, NOT a bug. The check is `S && S.pushOnSaveEnabled !== false` (index.html:20881); the FAKE fixture sets `pushOnSaveEnabled:false` (walk.json:3), so the self-test correctly fires. It is detecting the intended fixture opt-out.

---

## Summary of rulings

- **CONFIRM:** 1 (darwin-A), 2 (darwin-B), 3 (log-transaction, incl. the "missing-picker-not-broken-crediting" implication), 5 (plan-lock canonical path), 6 (ai-provenance, with raw-balance nuance enforced).
- **UPGRADE:** 4 (bills-mark-paid) from "CANDIDATE (walker maybe drove wrong affordance)" to **CONFIRMED bug** — the real `undoBillPaid()` modal button shares the same today's-month reader key (:8812) as the walker's `unmarkBillFromCal` path, so the day<payday cycle-bump key mismatch breaks BOTH undo affordances.
- **No DOWNGRADES.** No verdict was found over-claimed in direction (none were softened to ⊘).
- **Over-claims caught (framing, not direction):** (i) verdict 6 must NOT say the balance was wrong on this fixture — `bal_divergence=0` here; the raw read is a structural risk, the live bites are spend-power/id-miss/date/disclaimer. (ii) verdict 5 must NOT clear the whole lock surface — only the canonical unlock path was walked; the legacy/UI 2nd path stays an unwalked candidate.

**Single most important correction:** Verdict 4 should be **upgraded, not parked as a maybe-walker-artifact**. The brief's challenge ("did the walker drive the wrong undo?") was the right question, and the answer is: no — the genuine user-facing `undoBillPaid()` modal button (index.html:8809-8812) reads the same today's-month key `2026-5-Phone Plan-10` that misses the cycle-bumped writer key `2026-6-Phone Plan-10`. For any day<payday bill, the user's own Undo silently no-ops, leaving the bill stuck-paid and the $50 unrecovered. This is a real writer-key-vs-reader-key bug.
