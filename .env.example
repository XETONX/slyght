# Bundle 30.5 Phase C — visual verification env config.
#
# Copy to .env (gitignored) and fill in real values. Used by
# scripts/verify-visual-state.js. If ANTHROPIC_API_KEY is unset, the
# verify script runs in manual-review mode (no API calls; every capture
# flagged for human review).

# Anthropic API key for Haiku auto-flag pass (Q-30.5.1 hybrid answer).
# Get one at: https://console.anthropic.com/settings/keys
# Cost target: ~$0.10 per full smoke run (24 captures × ~$0.003 each).
ANTHROPIC_API_KEY=
